# and combines boolean expressions.

val a = true
val b = true

if a and b
    print "both"
end

val hp = 10
val food = 5

if hp > 0 and food > 0
    print "alive"
end

val has_key = true
val door_locked = false
print has_key and door_locked
